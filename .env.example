# =============================================================================
# SaaS Template Environment Variables
# =============================================================================
# 1. Copy this file: cp .env.example .env.local
# 2. Fill in your actual values below
# 3. Never commit .env.local to git (it's already gitignored)
# =============================================================================

# -----------------------------------------------------------------------------
# App URLs
# -----------------------------------------------------------------------------
BASE_URL="http://localhost:3000"
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# -----------------------------------------------------------------------------
# Supabase (Get from: https://supabase.com → Your Project → Settings → API)
# -----------------------------------------------------------------------------
NEXT_PUBLIC_SUPABASE_URL="https://your-project-id.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-supabase-anon-key"
# For Supabase server-side
SUPABASE_SERVICE_ROLE_KEY="your-supabase-service-role-key"

# -----------------------------------------------------------------------------
# Stripe (Get from: https://stripe.com → Developers → API Keys)
# -----------------------------------------------------------------------------
STRIPE_SECRET_KEY="sk_test_your_stripe_secret_key"
STRIPE_PRICE_ID="price_your_stripe_price_id"
STRIPE_WEBHOOK_SECRET="whsec_your_stripe_webhook_secret"
STRIPE_PRODUCT_NAME="stripe_product_name"

# -----------------------------------------------------------------------------
# Environment
# -----------------------------------------------------------------------------
NODE_ENV="development"

# =============================================================================
# Quick Setup Guide:
# =============================================================================
# 
# 🚀 SUPABASE SETUP:
# 1. Create project at https://supabase.com
# 2. Copy URL and anon key from Settings → API
# 3. Run schema: Copy lib/db/schema.sql into SQL Editor and execute
# 4. Generate types: npm run db:types
#
# 💳 STRIPE SETUP:
# 1. Create account at https://stripe.com
# 2. Use test keys (sk_test_*) for development
# 3. Create a product and copy the price ID
# 4. For webhooks: https://yourdomain.com/api/stripe/webhook
#
# 🚀 PRODUCTION:
# Set these same variables in your deployment platform:
# - Vercel: Project Settings → Environment Variables
# - Railway: Dashboard → Variables
# - Remember to use LIVE Stripe keys (sk_live_*) for production!
#
# =============================================================================
